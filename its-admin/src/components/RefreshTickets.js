import React, { Component } from 'react';

class RefreshTickets extends Component {
    render() {
        return (
            <button className="btn btn-success">
                <i className="fa fa-refresh"></i> Refresh
            </button>
        );
    }
}

export default RefreshTickets;